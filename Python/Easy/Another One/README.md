# 🐍 Plus One to Number Represented as Digits

## 📌 Problem Statement
You are given an integer array **digits**, where each element represents a digit of a positive whole number.

- Each `digits[i]` is the **i-th digit**
- Digits are ordered from **most significant** to **least significant**

Your task is to return the array of digits after **adding one** to the number.

---

## 📝 Example

### Example 1

**Input**

```python
digits = [1, 2, 3]
