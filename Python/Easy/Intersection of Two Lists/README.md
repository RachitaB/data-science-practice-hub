# 🐍 Intersection of Two Lists

## 📌 Problem Statement
Write a function to find the **intersection of two lists**.

The intersection should contain **only the elements present in both lists**.

---

## 📝 Definition
The **intersection** of two lists means all common elements shared between them.

---

## 🧾 Example Input

```python
A = [1, 2, 3, 4, 5]
B = [0, 1, 3, 7]
